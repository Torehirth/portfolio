import MenuSharpIcon from "@mui/icons-material/MenuSharp";

const HamburgerMenu = () => {
  return (
    <MenuSharpIcon
      aria-label="Hamburger menu"
      className="text-primaryBlack dark:text-primaryWhite hover:scale-105 cursor-pointer"
    />
  );
};

export default HamburgerMenu;
